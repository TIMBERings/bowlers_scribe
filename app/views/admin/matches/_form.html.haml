= simple_form_for @admin_match do |f|
  - if @admin_match.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_match.errors.count, "error")} prohibited this admin_match from being saved:"
      %ul
        - @admin_match.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
