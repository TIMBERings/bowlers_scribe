= form_for @admin_season do |f|
  - if @admin_season.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_season.errors.count, "error")} prohibited this admin_season from being saved:"
      %ul
        - @admin_season.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
