= form_for @admin_game do |f|
  - if @admin_game.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_game.errors.count, "error")} prohibited this admin_game from being saved:"
      %ul
        - @admin_game.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
